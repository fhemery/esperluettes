@props(['activity' => null])

<div class="surface-bg p-6 rounded-lg flex flex-col gap-4">
    FAKE CONFIG PANEL
    <input type="text" name="plugin_label" value="{{ old('plugin_label', '') }}" />
</div>
