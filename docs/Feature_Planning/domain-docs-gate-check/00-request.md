# Domain docs gate check — request

*Written by the user. Free form, may be three lines. Everything below is
optional prompting, not a form to fill.*

## What I want

Check inside the gate (`npm run gate`, docs part) that every domain under
`app/Domains` is documented: it must have a `README.md`, an `AGENTS.md`, and a
`CLAUDE.md`, as the `document-domain` skill produces.

Also check inside the gate that every domain has an entry in the root
`AGENTS.md` Domain Registry table, so that no domain is ever lost from sight.

## Why

<the problem it solves, or the user it serves>

## Constraints or ideas I already have

<anything that should not be re-litigated during REFINE>

## Explicitly out of scope

<what you already know you do not want>
