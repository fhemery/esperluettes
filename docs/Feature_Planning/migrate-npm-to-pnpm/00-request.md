# Migrate npm to pnpm — request

*Written by the user. Free form, may be three lines. Everything below is
optional prompting, not a form to fill.*

## What I want

Migrate the project's package manager from npm to pnpm.

## Why

NPM is unsafe actually and sensible to a lot of attacks. Pnpm prevents download versions that have been published less than 24 hours ago by default.

## Constraints or ideas I already have

Document how to setup PNPM in the installation. Also add an ADR explaining the choice of NPM and the consequences (mostly an additional installation instruction).

## Explicitly out of scope

(none given)
