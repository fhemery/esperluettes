# Favicon stays the same when season changed — request

*Written by the user. Free form, may be three lines. Everything below is
optional prompting, not a form to fill.*

## What I want

favicon stays the same when season changed. This is probably because there is no suffix on the favicon to refresh the cache, but I don't know if we can add a query parameter. If we can, then "?theme=<season>" would be perfect to ensure favicon rotation

## Why

## Constraints or ideas I already have

If we can add a query parameter, `?theme=<season>` would be perfect to ensure favicon rotation.

## Explicitly out of scope
