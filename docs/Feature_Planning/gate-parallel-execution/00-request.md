# Gate parallel execution — request

*Written by the user. Free form, may be three lines. Everything below is
optional prompting, not a form to fill.*

## What I want

`npm run gate` seems to try to launch sequential PHP tests on more than a few
domains, causing the sequential tests to be slower than testing everything in
parallel on multi-core machines. Investigate and check why this behaviour is
happening and optimize the gate for the PHP part. Also, check the possibility
to run all checks in parallel (JS, doc, etc...).

## Why

<the problem it solves, or the user it serves>

## Constraints or ideas I already have

<anything that should not be re-litigated during REFINE>

## Explicitly out of scope

<what you already know you do not want>
