---
description: Resume the in-progress task in the loop-engineering backlog
---

Invoke the `continue-task` skill and follow it. It is the orchestrator of the
loop defined in `.agents/loop/README.md`.

$ARGUMENTS
